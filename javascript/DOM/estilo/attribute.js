//Atributos
//Manipulando elementos

const header = document.querySelector('header');
header.setAttribute('id', 'header')
header.setAttribute('class', 'bg headr')
header.removeAttribute('id')
header.removeAttribute('class', 'headr' )